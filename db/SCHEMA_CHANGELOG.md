# Schema Check Log

- 2025-10-26: Verified canonical tables exist (`frameworks`, `controls`, `mappings`, `mapping_references`). Added non-destructive tables and indexes for `audits` and `evidence`. See `schema.sql`.

